import { DescriptionFilterPipe } from './description-filter.pipe';

describe('DescriptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
